# Assets

Esta carpeta es opcional. Puedes colocar aquí recursos estáticos adicionales
(por ejemplo, una versión del logo en PNG/SVG para usar como favicon o en
capturas de pantalla). La aplicación no depende de ningún archivo dentro de
esta carpeta: el logo que se ve en el login y en el sidebar se genera con
código SVG directamente en `js/app.js`.
